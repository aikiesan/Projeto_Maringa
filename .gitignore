__pycache__/
*.pyc
.DS_Store
*.tmp
database/maringa_project.db

# ---------------------------------------------------------------------------
# Acervo sob TCLE — NUNCA versionar. Contém nomes, cargos e instituições
# nominais dos participantes (Anexo 02). A camada anonimizada autoritativa é
# codebook/interviews.csv; a identificação vive apenas em participants_vault,
# no banco local.
# ---------------------------------------------------------------------------
transcricoes/
DRIVE_FILES/
Termos*/
Registros*/
*_Transcrição.docx
*_Transcricao.docx
*_Resumo.docx
*_Gravação.mp4
*_Gravacao.mp4
Lista de Entrevistas.xlsx
lista de convidados*
saida_triagem/
*.triagem.md

# ambiente local
.env
pgdata/
*.log

# identificação dos participantes (participants_vault) e saída de triagem
vault/
saida_triagem/

# saída da anonimização das transcrições (tools/anonimizar_transcricoes.py).
# Os anexos são anonimizados, mas o relatório e o revisar.csv citam nomes e
# trechos do texto original — nada disso vai ao repositório.
saida_anonimizacao/

# saída do anexo de entrega (tools/transcricoes_entrega.py): as 17 transcrições
# com os cortes dentro do arquivo. São transcrições de acervo, mesma regra de
# saida_anonimizacao/ — não vão ao repositório.
saida_entrega/

# contatos nominais dos pontos focais (Produto 3) e saídas do painel que os embutem
data/contacts.json
docs/data/contacts.json
static/data/contacts.json
/index.html
public/index.html
P3_Base_Mapeamento_Atores_Maringa.xlsx

# binários sem relação com o projeto
*.exe
*.winmd
show*.pdf

# senha do painel publicado (usada por tools/lock.py)
.senha

# ---------------------------------------------------------------------------
# Anexos do Produto 4 (sessão de 12/09).
#
# O .docx do produto É entregável e fica versionado. O que não fica:
#   Anexo_05...zip     transcrições anonimizadas — mesma regra de
#                      saida_anonimizacao/: histórico de git não se apaga
#   cortes.csv         guarda o texto autoidentificador que foi SUPRIMIDO de
#                      ENT-008, ENT-012 e ENT-015. É o oposto de anonimizado.
#   revisao_nomes.csv  nomes próprios de terceiros citados em entrevista
# ---------------------------------------------------------------------------
anexos/Anexo_05_Transcricoes_Anonimizadas.zip
anexos/cortes.csv
anexos/revisao_nomes.csv

# saída aberta do painel: gerada por tools/publico.py a partir de
# site/artifact.html, pela mesma razão que /index.html e public/index.html
site/publico.html

# inspeção pontual e restos locais
_inspecao/
.wrangler/
Nova pasta/

# saida gerada do Hub publico (espelho de aikiesan/Maringa) — regenerada por
# `python -m tools.hub.build`; o que vale versionar e o gerador, em tools/hub/
hub_saida/

# capturas de tela do Hub. A varredura le .html/.csv/.json/.css e NAO le imagem:
# um nome dentro de um PNG passa pelo portao sem ser visto. conselhos.png mostra
# a composicao nominal do COMDEMA.
Claude outputs/*.png

# indice literal do que foi purgado do historico
purge.txt

# copia de 328 paragrafos do Produto 4 (sem cap. 4, Consideracoes e Anexos).
# A fonte e anexos/Produto_4.docx, 367 paragrafos. Mantida so para nao sumir.
Produto_4_v328_OBSOLETO_NAO_USAR.docx

# pagina local de revisao das ancoras: mostra afirmacao ao lado de trecho e
# parafrase, material de trabalho. O CSV de revisao e que e versionado.
revisao_ancoras.html
revisao_pendentes.html

# manual de identidade do projeto: 11 slides de 5334x3000, ~5 MB no total.
# O que o Hub usa sao os recortes em assets/marca/, versionados.
MARINGÁ EM AÇÃO PELO CLIMA - LOGO ID/

# auditoria de autoidentificacao de empregador: o contexto traz trecho de
# transcricao, como cortes.csv e revisao_nomes.csv. Nao versionar.
anexos/revisao_local_trabalho.csv
